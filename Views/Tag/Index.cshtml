
<h1> Tags </h1>

<div class="w3-card">
    @foreach (var tag in Model)
    {
        <header div class="w3-container w3-yellow">
            <a asp-controller="Tag" asp-action="Products" asp-route-id="tag.Id"> @tag.Name </a>
        </header>

    }
</div>